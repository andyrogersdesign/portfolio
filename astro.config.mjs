// @ts-check
import { defineConfig } from 'astro/config';

// Your live domain. Used to build absolute URLs (sitemaps, social tags, etc.).
export default defineConfig({
  site: 'https://andyrogers.design',
});
